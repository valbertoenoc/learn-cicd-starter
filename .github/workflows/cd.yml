on:
  pull_request:
    branches: [main]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      name: Check out code
      uses: actions/checkout@v6

      name: Set up go
      uses: actions/setup-go@v6
      with:
        go-version: "1.27.1"

      name: Build
      run: ./scripts/buildprod.sh
